"use strict";

const angular = require("angular");

angular.module("mixtape").controller("FavoritesCtrl", function($scope) {
    // TODO: grab media cards and user avatars for favorites, sort by recently interacted with
});